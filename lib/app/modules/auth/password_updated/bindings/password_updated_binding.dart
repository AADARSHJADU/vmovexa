import 'package:get/get.dart';

class PasswordUpdatedBinding extends Bindings {
  @override
  void dependencies() {
    // No controller needed for static success page, but we maintain the binding structure
  }
}
